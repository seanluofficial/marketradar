# Runtime dependencies for the Streamlit Community Cloud deployment.
# The app imports the `radar` package from the repository root (added to sys.path in
# radar/app/main.py), so the package itself is NOT installed -- only these libraries.
# Mirrors the runtime deps in pyproject.toml; keep the two in sync.
streamlit>=1.50
pandas>=2.2
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
networkx>=3.1
pyarrow>=14.0
matplotlib>=3.8
python-dotenv>=1.0
requests>=2.32
